// instrução é qualquqer códico que a gente passa na tela do nosso editor de texto

// ex:

let nome = 'Jeorgel';

console.log(2 + 3);

/* 
cometários => servem para descrever um pequeno bloco de código para os desenvolvedores se orientarem 
quando estiverem a usar ou retificar a nossa app.

existem 2 tipos de comentários: usando (//) e usando "/**
/"
*/
// ex:

// funcão que soma dois números:
function soma() {
    console.log(10 + 11);
}

/*
declacração de variável
com tipo de dados parseInt
e
com o tipo de string
*/

const numero = parseInt(10);
const apelido = toString(jeorgel); 

// casesensitive => o js é uma linguagem case sentsite porque deferencia maiúsculas de minúsculas

// ex:

const N1 = 2;

console.log(n1) // o resulatdo será um erro undefined, pois, a variável declarada é diferente da que foi chamada.

//  cero seria:
console.log(N1);